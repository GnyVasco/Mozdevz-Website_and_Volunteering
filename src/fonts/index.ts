import { Geist_Mono, Onest, Roboto } from "next/font/google";

export const roboto = Roboto({
    weight: "variable",
    subsets: ["latin"],
    variable: "--font-roboto",
});

export const geist_mono = Geist_Mono({
    weight: "variable",
    subsets: ["latin"],
    variable: "--font-geist-mono",
});

export const onest = Onest({
    weight: "variable",
    subsets: ["latin"],
    variable: "--font-onest",
});
